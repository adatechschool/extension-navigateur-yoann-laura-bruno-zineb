// service-worker.js

self.addEventListener("install", (event) => {
  // Effectuez des opérations d'installation ici
});

self.addEventListener("activate", (event) => {
  // Effectuez des opérations d'activation ici
});

// self.addEventListener('fetch', (event) => {
//   // Interceptez les requêtes réseau ici
// });
